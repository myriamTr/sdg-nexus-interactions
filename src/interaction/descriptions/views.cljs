(ns interaction.descriptions.views
  (:require [bulma-cljs.core :as b]
            [re-frame.core :as rf]
            [reagent.core :as reagent]
            [reagent.debug :as rd]))

(defn descriptions-view []
  [:div])
